{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "PYTHONPATH=src:. python -m uvicorn ui.api.main:app --host 0.0.0.0 --port ${PORT:-8000}",
    "healthcheckPath": "/api/patterns"
  }
}
